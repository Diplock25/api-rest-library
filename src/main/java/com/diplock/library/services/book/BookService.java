package com.diplock.library.services.book;

public interface BookService {
  // Estos archivos solo son para ejemplificar nomenclatura.
  // En este caso, donde vayamos a tener una interfaz con su implementación, crearemos un package que englobe a los 2 archivos.
  // Si dentro de un package ya existen otros packages, aun que nosotros solo vayamos a crear una interfaz,
  // por limpieza crearemos un package para la interfaz, para quede uniforme.
}
